groups:
  default:
    options:
      default: true
    permissions:
      - betterrtp.rtp
      - betterrtp.world.world
default:
  permissions:
    multiverse.core.tp.self: true
    multiverse.access.*: true
